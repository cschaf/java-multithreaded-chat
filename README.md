# Multithreaded-Java-Chat
This project conatins a multithreaded chat server and client. The server can accept multiple clients  simultaneously and serves them. The client connects to chat server and prints all the messages  received from the server. It also allows the user to send messages to the  server.
